# CampusFlow Agentic Workspace

This repository houses the backend AI intelligence agent for the CampusFlow portal, utilizing LangChain and local LLM execution infrastructure.

## 🛠️ Project Setup

1. **Install Dependencies:**
   Ensure you have the required packages installed locally:
   ```bash
   pip install -r requirements.txt
